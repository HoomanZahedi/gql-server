type Query {
  jobs: [Job]
  companies: [Company]
  singleJob(id: ID!): Job
  singleCompany(id: ID!): Company
}

type Company {
  id: ID!
  name: String!
  description: String
  job: [Job!]
}

type Job {
  id: ID!
  title: String!
  description: String
  date: String
  company: Company!
}
